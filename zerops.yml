zerops:
  - setup: svelte
    build:
      prepareCommands:
        - curl -fsSL https://bun.sh/install | bash
        - bun --help
      buildCommands:
        - bun install
        - bun run build
      deployFiles:
        - ./build
        - package.json
    run:
      prepareCommands:
        - curl -fsSL https://bun.sh/install | bash
      envVariables:
        NODE_ENV: production
      ports:
        - port: 3000
          httpSupport: true        
      start: bun start
